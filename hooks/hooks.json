{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then exec python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/aidd.py\" hook pre-tool; else exec python \"${CLAUDE_PLUGIN_ROOT}/scripts/aidd.py\" hook pre-tool; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
