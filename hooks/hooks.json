{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "for c in python3 python py; do if \"$c\" -c \"import sys; sys.exit(0 if sys.version_info >= (3, 8) else 1)\" </dev/null >/dev/null 2>&1; then exec \"$c\" \"${CLAUDE_PLUGIN_ROOT}/scripts/aidd.py\" hook pre-tool; fi; done; exit 0",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
